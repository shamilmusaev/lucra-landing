---
import { getStrings, type Lang } from '../../i18n/utils';
import Icon from '../Icon.astro';
const { lang } = Astro.props as { lang: Lang };
const s = getStrings(lang);
---

<section id="sakerhet" class="tone-slate">
  <div class="container">
    <div class="security">
      <div class="reveal">
        <span class="section-eyebrow">{s.sakerhetEyebrow}</span>
        <h2 class="section-title">{s.sakerhetTitle}</h2>
        <p class="section-sub">
          {s.sakerhetSub}
        </p>
      </div>
      <div class="sec-list">
        <div class="sec-item reveal">
          <div class="sec-icon">
            <Icon name="shield-check" />
          </div>
          <div>
            <h4>{s.sakerhetCard1Title}</h4>
            <p>{s.sakerhetCard1Body}</p>
          </div>
        </div>
        <div class="sec-item reveal delay-1">
          <div class="sec-icon">
            <Icon name="lock" />
          </div>
          <div>
            <h4>{s.sakerhetCard2Title}</h4>
            <p>{s.sakerhetCard2Body}</p>
          </div>
        </div>
        <div class="sec-item reveal delay-2">
          <div class="sec-icon">
            <Icon name="globe" />
          </div>
          <div>
            <h4>{s.sakerhetCard3Title}</h4>
            <p>{s.sakerhetCard3Body}</p>
          </div>
        </div>
        <div class="sec-item reveal delay-3">
          <div class="sec-icon">
            <Icon name="key-round" />
          </div>
          <div>
            <h4>{s.sakerhetCard4Title}</h4>
            <p>{s.sakerhetCard4Body}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
#sakerhet {
  padding-top: 96px;
  padding-bottom: 96px;
}

.security {
  display: grid;
  grid-template-columns: 1fr 1.1fr;
  gap: 64px;
  align-items: start;
}

.sec-list {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.sec-item {
  display: flex;
  align-items: flex-start;
  gap: 18px;
  padding: 22px 24px;
  background: var(--bg-paper);
  border: 1px solid var(--border);
  border-radius: var(--radius-xl);
  transition: border-color .2s, transform .2s;
}

.sec-item:hover {
  border-color: var(--slate-300);
  transform: translateY(-2px);
}

.sec-icon {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  background: linear-gradient(150deg, #ffffff, var(--accent-light));
  border: 1px solid rgba(61, 80, 109, .10);
  box-shadow: 0 1px 2px rgba(15, 23, 42, .05), inset 0 1px 0 rgba(255, 255, 255, .7);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  color: var(--brand-primary);
}

.sec-icon svg {
  width: 22px;
  height: 22px;
}

.sec-item h4 {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 600;
  color: var(--brand-primary);
  margin: 0 0 6px;
}

.sec-item p {
  font-size: 14px;
  color: var(--slate-500);
  margin: 0;
  line-height: 1.55;
}

@media (max-width: 960px) {
  .security {
    grid-template-columns: 1fr;
    gap: 40px;
  }
}
</style>
